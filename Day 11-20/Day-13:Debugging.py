#Debugging

pages = 0
words_per_page = 0
pages = int(input("Number of pages:"))
words_per_page = int(input("Words per page:"))
total_words = pages * words_per_page

#Printing out values to confirm them
print(f"pages = {pages}")
print(f"words per page = {words_per_page}")

print(total_words)
